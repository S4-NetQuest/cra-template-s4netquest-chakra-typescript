import { ChakraTheme } from '@chakra-ui/react';

const shadows: ChakraTheme['shadows'] = {
  'dark-md': '2px 2px 8px 1px rgba(0,0,0,0.4)',
  'md-up': '0 -4px 6px -1px rgba(0, 0, 0, 0.1), 0 -2px 4px -1px rgba(0, 0, 0, 0.06)',
};

export default shadows;
